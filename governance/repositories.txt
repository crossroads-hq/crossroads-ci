# Reviewed Crossroads fleet list for scripts/apply-fleet.sh and
# scripts/verify-fleet.sh.
#
# Format, one repository per line, whitespace-separated:
#
#   owner/repository  profile  branch
#
# profile is one of:
#   full       ruleset-full.json    — requires the "PR Validation" status check.
#                                     Only for repositories that HAVE a gate job
#                                     with that exact name: requiring a check
#                                     that never reports blocks every pull
#                                     request permanently, with no escape,
#                                     because bypass_actors is empty and
#                                     administrators are enforced.
#   full+tags  ruleset-full.json + tag-ruleset.json — as above, plus v* release
#                                     tags become immutable. For repositories
#                                     that publish from a tag push.
#   minimal    ruleset-minimal.json — pull-request-only merges, no force-push,
#                                     no deletion, but NO required status check.
#                                     For repositories with no CI. This profile
#                                     exists precisely so "no CI yet" stops
#                                     being a reason to have no protection at
#                                     all.
#
# Blank lines and comments are ignored.

# Control plane — this repository governs itself once its CI gate exists
crossroads-hq/crossroads-ci                 full       main

# Tier A — published npm packages
crossroads-hq/storybook-live-code-sandbox   full+tags  main
crossroads-hq/storybook-live-code-addon     full+tags  main

# Tier B — private applications
crossroads-hq/crossroads-ui                 full       main
crossroads-hq/crossroads-evolution          full       main

# Tier C — docs / contract repositories
crossroads-hq/crossroads-agent-system       full       main
# Vault sanity gate (conflict markers + JSON validity) added 2026-08-23 once
# Actions billing cleared; the aggregate check name matches the fleet, so the
# full profile applies. NOTE: full ends direct pushes to the vault branch —
# every Obsidian sync now lands via a pull request. If that turns out to be
# unlivable, flip this back to `minimal` and re-run apply-fleet; the gate
# workflow keeps running either way. Non-standard default branch.
crossroads-hq/crossroads-os                 full       crossroads-os-v2

# Tier D — live but CI-less. `minimal` until archived or promoted.
crossroads-hq/evolution-demo                minimal    main
crossroads-hq/Product-management-OS         minimal    main
crossroads-hq/resume-adaptation-assistant   minimal    main
crossroads-hq/designer-skills               minimal    main
# Excluded, with reasons:
#
#   storybook-live-code — fork of storybookjs/storybook. Its workflows and
#     branch model are upstream's; governing it here would fight the fork.
#   mini-rag, receipt-smart-sync, personality-insights-nodejs,
#   apigw-sample-app — ARCHIVED 2026-08-24 (most recent last-commit dates range from 2015 to
#     Feb 2026). Archiving freezes a repository entirely and supersedes any
#     ruleset; unarchive first if one ever comes back, then re-add here.
